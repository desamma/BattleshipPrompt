@model Battleship.Models.TwoPlayerGameState

@{    ViewData["Title"] = "Battleship - Two Player";}

<div class="row">
    <div class="col-md-6">
        <h2>Player 1's Board</h2>
        <div class="board">
            @for (int i = 0; i < GameBoard.Size; i++)
            {
                <div class="row">
                    @for (int j = 0; j < GameBoard.Size; j++)
                    {
                        if (!Model.IsPlayer1Turn)
                        {
                            <form asp-action="Attack" asp-controller="TwoPlayerGame" method="post">
                                <input type="hidden" name="row" value="@i" />
                                <input type="hidden" name="col" value="@j" />
                                <button type="submit" class="cell @Model.Player1Board.GetCellClass(i, j, true)"></button>
                            </form>
                        }
                        else
                        {
                            <div class="cell @Model.Player1Board.GetCellClass(i, j, true)"></div>
                        }
                    }
                </div>
            }
        </div>
    </div>
    <div class="col-md-6">
        <h2>Player 2's Board</h2>
        <div class="board">
            @for (int i = 0; i < GameBoard.Size; i++)
            {
                <div class="row">
                    @for (int j = 0; j < GameBoard.Size; j++)
                    {
                        if (Model.IsPlayer1Turn)
                        {
                            <form asp-action="Attack" asp-controller="TwoPlayerGame" method="post">
                                <input type="hidden" name="row" value="@i" />
                                <input type="hidden" name="col" value="@j" />
                                <button type="submit" class="cell @Model.Player2Board.GetCellClass(i, j, true)"></button>
                            </form>
                        }
                        else
                        {
                            <div class="cell @Model.Player2Board.GetCellClass(i, j, true)"></div>
                        }
                    }
                </div>
            }
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12 text-center">
        <h3>@Model.Message</h3>
        @if (Model.IsGameOver)
        {
            <a asp-action="NewGame" class="btn btn-primary">Play Again</a>
        }
    </div>
</div>
